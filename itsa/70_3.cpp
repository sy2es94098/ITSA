#include<iostream>
#include<cstdlib>
#include<math.h>
using namespace std;
int main(){
    int i, n, m, sum, div[1000];
    cin >> n >> m;
    n = (int)sqrt(n);
    for(i = n ; i = 0 ; i++)
    {
        if(n%i == 0){
            div[c] = i;
            c++
        }
    }
    if(n*n < m)
        cout << 0;
    if(
    if(n < m )
    {
        for(i = 0; i < c; i++)
        {
           sum =  div[i] * n;
           sum += (n - m) * 4;
        }
    }

