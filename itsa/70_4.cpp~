#include<iostream>
#include<cstdlib>
using namespace std;
int main (){
    int i,n,m,f[20],o[50];
    cin >> n >> m ;
    for(i = 0; i < n ; i++)
    {
        cin >> f[i];
    }
    for(i = 0; i < m ; i++)
    {

    }

